name: "Spring AI Maven - BoringCache"

on:
  workflow_dispatch:
    inputs:
      cli_ref:
        description: "Optional boringcache/cli ref to build and use instead of the released CLI"
        required: false
        type: string
  push:
    branches:
      - main
    paths:
      - ".gitmodules"
      - "upstream"
  pull_request:
    paths:
      - ".gitmodules"
      - ".github/workflows/spring-ai-maven-boringcache.yml"
      - ".github/workflows/spring-ai-maven-actions-cache.yml"
      - ".github/workflows/reusable-maven-benchmark.yml"
      - "README.md"
      - "scenarios/**"
      - "scripts/**"
      - "upstream"
  schedule:
    - cron: "0 2 * * *"

permissions:
  contents: read
  actions: read

jobs:
  benchmark:
    uses: ./.github/workflows/reusable-maven-benchmark.yml
    with:
      benchmark_id: spring-ai-maven
      project_repo: spring-projects/spring-ai
      strategy: boringcache
      java_version: "21"
      build_command: ./mvnw --batch-mode -ntp clean install -DskipTests -Dmaven.javadoc.skip=true
    secrets: inherit
